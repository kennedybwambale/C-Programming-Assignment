#include <stdio.h>
 int x=1;//the termination used was wrong. Right one was ;

int main()
{ if(x=1)
   {

    printf(" x equals 1");
   }else{//not otherwise
       printf(" x does not equal 1");

   }
    return 0;
}
